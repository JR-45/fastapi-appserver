[project]
name = "h_world"
version = "0.1.0"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.42.0",
]

[project.scripts]
h_world = "h_world:main"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"