info:
  name: Suche nach ID
  type: graphql
  seq: 2

graphql:
  method: POST
  url: "{{restUrl}}"
  body:
    query: |
      {
        mitglied(mitgliedId: "1") {
          id
          vorname
          nachname
          email
          geburtsdatum
          telefonnummer
        }
      }

runtime:
  variables:
    - name: restUrl
      value: https://127.0.0.1:8000/graphql

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
